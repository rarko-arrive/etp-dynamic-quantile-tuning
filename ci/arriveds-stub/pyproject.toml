[project]
name = "arriveds"
version = "0.0.0+ci"
description = "Minimal arriveds stub for GitHub Actions (no Snowflake)"
requires-python = ">=3.12"

[project.optional-dependencies]
azure = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/arriveds"]
